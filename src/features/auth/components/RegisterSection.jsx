"use client";

import RegisterForm from "./RegisterForm";

const RegisterSection = () => {
  return (
    <section className="w-screen h-screen flex justify-center items-center bg-secondary">
      <RegisterForm />
    </section>
  );
};

export default RegisterSection;
